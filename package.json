{
  "name": "programming-teacher",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web",
    "bundle:server": "node scripts/bundle-server.mjs",
    "start": "npm run start -w server",
    "test": "vitest run",
    "lint-content": "tsx scripts/lint-content.ts",
    "app:pack": "npm run build && npm run bundle:server && node scripts/stage-server-deps.mjs && node scripts/make-icon.mjs",
    "app": "npm run start --prefix app",
    "app:dist": "npm run app:pack && npm run dist --prefix app && node scripts/pack-zip.mjs && node scripts/make-shortcut.mjs",
    "app:zip": "node scripts/pack-zip.mjs",
    "app:shortcut": "node scripts/make-shortcut.mjs"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
