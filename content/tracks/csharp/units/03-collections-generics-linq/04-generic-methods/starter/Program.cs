List<int> numbers = new List<int> { 3, 9, 27, 81, 243 };
List<string> words = new List<string> { "alpha", "beta", "gamma" };

// Print First(numbers), First(words), Last(numbers), Last(words) — one per line.

// Define below (each written ONCE, generic over T):
//   T First<T>(List<T> items) — returns the item at index 0
//   T Last<T>(List<T> items)  — returns the item at index Count - 1
