from pathlib import Path

# 1. Make a Path called journal pointing at "journal.txt".

# 2. Write the first line with write_text: "Day 1: learned classes\n"

# 3. Append with open(journal, "a", ...): "Day 2: survived exceptions\n"

# 4. Read the file back with read_text, print each line, then print
#    f"{len(lines)} lines saved".
