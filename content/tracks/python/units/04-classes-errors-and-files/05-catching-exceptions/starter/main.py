entries = ["12", "7", "extra cheese", "30"]

total = 0

# 1. Loop over entries, adding int(entry) to total inside a try.

# 2. except ValueError: print(f"Skipping: {entry}") — and keep going.

# 3. After the loop, print(total).
