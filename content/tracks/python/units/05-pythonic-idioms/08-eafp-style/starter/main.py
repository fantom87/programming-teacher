pantry = {"flour": 2, "eggs": 12}

# 1. parse_age(text) — TRY to return int(text); if that raises
#    ValueError, return None. No pre-checking.

# 2. lookup(stock, item) — TRY to return stock[item]; if that raises
#    KeyError, return 0.

# 3. Print parse_age("42"), parse_age("forty-two"),
#    lookup(pantry, "flour"), lookup(pantry, "saffron").
