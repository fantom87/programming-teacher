# Two functions, each body a single one-line list comprehension:

# 1. to_fahrenheit(temps) — RETURN a new list with every Celsius
#    temperature converted: t * 9 / 5 + 32.

# 2. long_words(words, n) — RETURN only the words longer than n letters.

temps_c = [0, 10, 22, 31, 17]
words = ["idiom", "loop", "comprehension", "list", "pythonic"]

# Print to_fahrenheit(temps_c), then long_words(words, 6).
