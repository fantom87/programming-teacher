# 1. Iterators by hand: letters = iter("abc"), then print next(letters)
#    twice — watch the cursor move.

# 2. countdown(n) — a GENERATOR (use yield) producing n, n-1, ... 1.

# 3. Loop over countdown(3) printing each number, print "Liftoff!",
#    then print sum(countdown(100)).
