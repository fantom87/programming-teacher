# Two functions, two curly-brace comprehensions:

# 1. word_lengths(words) — RETURN a dict mapping each word to its
#    length: {"zip": 3, ...}

# 2. unique_initials(names) — RETURN a SET of each name's first letter.

tools = ["idiom", "generator", "zip"]
crew = ["ada", "alan", "grace", "guido", "linus"]

# Print word_lengths(tools), then sorted(unique_initials(crew)).
