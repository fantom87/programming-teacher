# The program. Run it: a "Demo:" line sneaks in above the real
# greetings — it comes from the import. Fix it in greetings.py;
# this file needs no changes.

from greetings import make_greeting

print(make_greeting("Bradley"))
print(make_greeting("Ada"))
