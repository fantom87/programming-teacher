# textkit.py — your text-statistics library. Three documented tools:

# 1. count_words(text) — RETURN how many words the text has.
#    (text.split() turns a sentence into a list of words.)

# 2. longest_word(text) — RETURN the longest word (first wins a tie).

# 3. shout(text, punctuation="!") — RETURN the text uppercased with
#    punctuation on the end. punctuation defaults to "!".

# Give every function a one-line docstring.

# Demo — put these under a __main__ guard so imports stay silent:
#   print("textkit demo")
#   print(shout("it works"))
