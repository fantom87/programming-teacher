# This file is a MODULE — a toolbox other files can import.

# 1. banner(text) — RETURN the text wrapped in markers:
#      banner("Hi")  ->  "== Hi =="

# 2. underline(text) — RETURN one dash per character:
#      underline("Hi")  ->  "--"        (hint: "-" * len(text))
