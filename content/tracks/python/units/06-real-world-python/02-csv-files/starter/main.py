import csv

# 1. Read scores.csv with csv.DictReader — open it with
#    encoding="utf-8" and newline="".
#    Each row is a dict of STRINGS: convert with int() before adding.
#    Print each player's total:  Mira: 85

# 2. Write totals.csv with csv.writer (same open flags, mode "w"):
#    a ["name", "total"] header row, then one row per player.

# 3. Print: totals.csv written
