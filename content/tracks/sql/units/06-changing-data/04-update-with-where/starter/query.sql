-- Ethiopian beans got more expensive: every one of them goes up $1.50.

-- 1. LOOK FIRST. Select id, name, price for the rows you're about to
--    change, ordered by id. Same WHERE you'll use for the UPDATE.


-- 2. Now the UPDATE — same WHERE, new price computed from the old one.


-- 3. Verify: id, name, price for the whole shelf, ordered by id.
