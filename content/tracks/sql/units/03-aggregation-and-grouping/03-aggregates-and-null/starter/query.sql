-- Want to see the raw rows? Uncomment, run, then comment it out again:
-- SELECT * FROM sales ORDER BY id;

-- One row, five columns, all about rating:
--   sales, rated, rating_sum, avg_rating,
--   avg_if_zero = rating_sum spread over EVERY row instead
