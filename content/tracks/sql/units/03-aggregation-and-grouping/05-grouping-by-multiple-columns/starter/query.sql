-- Want to see the raw rows? Uncomment, run, then comment it out again:
-- SELECT * FROM sales ORDER BY id;

-- One row per category-and-channel pair: category, channel,
-- sales (row count), items (qty summed). Sorted by category, then channel.
