-- Want to see the raw rows? Uncomment, run, then comment it out again:
-- SELECT * FROM sales ORDER BY id;

-- One row per category: the category, its number of sales (alias: sales),
-- and its revenue (alias: revenue). Sort so the rows can't wander.
