-- Want to see the raw rows? Uncomment, run, then comment it out again:
-- SELECT * FROM sales ORDER BY id;

-- Counter sales only. One row per category: category, sales, revenue.
-- Keep only the categories whose revenue is over 100. Sort by category.
