-- Want to see the raw rows? Uncomment, run, then comment it out again:
-- SELECT * FROM sales ORDER BY id;

-- One row out: how many sales rows there are (alias: sales),
-- and how many of them carry a rating (alias: rated).
