-- Want to see the raw rows? Uncomment, run, then comment it out again:
-- SELECT * FROM sales ORDER BY id;

-- One row per category: category, sales (rows),
-- distinct_items (how many different items), avg_price rounded to 2 decimals.
-- Sort by category.
