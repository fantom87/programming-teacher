-- Want to see the raw rows? Uncomment, run, then comment it out again:
-- SELECT * FROM sales ORDER BY id;

-- Rated sales only. One row per item: item, sales, revenue,
-- avg_rating rounded to 2 decimals. Keep only items sold at least twice.
-- Best revenue first.
