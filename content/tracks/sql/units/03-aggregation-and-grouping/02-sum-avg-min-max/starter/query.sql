-- Want to see the raw rows? Uncomment, run, then comment it out again:
-- SELECT * FROM sales ORDER BY id;

-- One row, four columns:
--   items_sold = every qty added up
--   avg_price  = the average unit_price
--   cheapest   = the smallest unit_price
--   priciest   = the largest unit_price
