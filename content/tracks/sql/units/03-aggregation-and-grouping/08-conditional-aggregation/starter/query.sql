-- Want to see the raw rows? Uncomment, run, then comment it out again:
-- SELECT * FROM sales ORDER BY id;

-- One row per category, no WHERE anywhere:
--   counter_items = qty sold at the counter
--   online_items  = qty sold online
--   unrated       = how many of that category's sales have no rating
-- Sort by category.
