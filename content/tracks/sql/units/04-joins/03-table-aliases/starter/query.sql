-- The same join as last lesson, dressed for readability:
--   * alias customers AS c and orders AS o
--   * qualify every column with its alias
--   * name the output columns customer, order_id, placed
--   * keep only customers whose city is Austin
--   * order by the order id

