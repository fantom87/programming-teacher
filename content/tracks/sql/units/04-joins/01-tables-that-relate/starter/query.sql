-- Open schema.sql first — it is the map of these two tables.

-- 1. List every customer: id, name, city. End with ORDER BY id.


-- 2. Mara's id appears in that first result. Use that number to pull her
--    orders (id, customer_id, order_date) out of the orders table.
--    End with ORDER BY id.

