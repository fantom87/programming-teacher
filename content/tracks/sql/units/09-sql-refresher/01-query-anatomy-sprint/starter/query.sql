-- Nightjar Records — query anatomy sprint.
-- schema.sql runs first, so all six tables already exist.
-- Write three queries, in this order. Every one needs a deterministic ORDER BY.

-- 1. Sale sheet: title, year, sale_price (10% off, two decimals)
--    for albums priced above 22, dearest first.


-- 2. The distinct countries in the artists table, sorted.


-- 3. id, title and price of the three priciest albums.
