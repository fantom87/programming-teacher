// 1. Write an arrow function `square` — takes n, returns n * n.

// 2. Write an arrow function `half` — takes n, returns n / 2.

// 3. Write an arrow function `shout` — takes a word, returns it
//    uppercased with "!" on the end.  shout("hi") -> "HI!"

// 4. Print square(5), half(10), and shout("hi") — one per line.
