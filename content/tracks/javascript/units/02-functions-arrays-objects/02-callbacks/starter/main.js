const addTen = (n) => n + 10;
const snacks = ["apples", "pretzels", "cocoa"];

// 1. applyTwice(fn, value) — call fn on value, then call fn on THAT
//    result, and RETURN it.

// 2. forEachItem(items, action) — loop items with for...of and call
//    action(item) for each one.

// 3. Print applyTwice(addTen, 5);   // 25

// 4. Use forEachItem to print `I like ${snack}` for every snack.
