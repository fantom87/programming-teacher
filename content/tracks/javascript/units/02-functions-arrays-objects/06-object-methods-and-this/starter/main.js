// 1. Create an object `hero` with name: "Ada", score: 0, and two methods:
//
//    describe()        — RETURN `${this.name} has ${this.score} points`
//    addPoints(points) — increase this.score by points
//
//    (Method shorthand:  describe() { ... },  — no `function` keyword,
//     and no arrows here: arrows don't get their own `this`.)

// 2. Award the points: call hero.addPoints(25) TWICE.

// 3. Print hero.describe();   // Ada has 50 points
