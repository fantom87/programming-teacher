const numbers = [1, 2, 3, 4, 5, 6];

// 1. doubleAll(nums) — RETURN a new array with every number doubled (.map).

// 2. evensOnly(nums) — RETURN a new array of just the even numbers (.filter).
//    Even means: n % 2 === 0

// 3. shoutAll(words) — RETURN every word uppercased with "!" on the end (.map).

// 4. Print doubleAll(numbers).join(", ") and evensOnly(numbers).join(", ").
