const dice = [4, 6, 1, 3];

const party = [
  { name: "Ada", score: 120 },
  { name: "Sam", score: 80 },
  { name: "Rin", score: 50 },
];

// 1. sum(numbers) — RETURN the total, using .reduce with a start value of 0.

// 2. totalScore(players) — RETURN the sum of every player's .score,
//    also with .reduce.

// 3. Print sum(dice) and totalScore(party).
