// The program. All the math lives in stats.js — import it.

const RUN_TIMES = [30, 50, 62, 41, 47];

// 1. One line: import summarize (the default) plus mean and median
//    (named) from "./stats.js".

// 2. Print `mean: ${...}`, `median: ${...}`, then summarize(RUN_TIMES).
