// replay.d.ts — the typed public face of replay.js. Declarations only:
// no bodies, no runtime code. Read replay.js first, then promise the
// compiler exactly what it does.

// 1. export interface Play — title: string, minutes: number.

// 2. Signatures for both functions (a declaration ends at the semicolon):
//    export function topTrack(plays: Play[]): string;
//    export function totalMinutes(plays: ...): ...;
