// main.js — the SHELL. The only file allowed to console.log, and it
// computes nothing itself.

// import { addRecord } from "./crates.js";
// import { crateReport } from "./report.js";

// Build the crate with three addRecord calls:
//   "Blue in Green" 37 min, "Aurora Suite" 52 min, "Night Drive" 41 min
// (each call returns a NEW crate — reassign as you go).

// Then loop crateReport(crate) and console.log each line.
