// report.js — the FORMATTING layer. Imports only from ./crates.js,
// returns strings, never prints.

// import { totalMinutes, longest } from "./crates.js";

// export function crateReport(crate) — return an ARRAY of two strings:
//   `${crate.length} records, ${totalMinutes(crate)} minutes`
//   `longest: ${longest(crate).title}`
// Every number must come from a core call — no local arithmetic.
