// crates.js — the CORE. Pure functions only: no imports, no console,
// no mutation. Data in, new data out.

// export function addRecord(crate, title, minutes) — return a NEW array
// with { title, minutes } appended (spread, never push).

// export function totalMinutes(crate) — reduce the minutes to one sum.

// export function longest(crate) — the record with the most minutes.
