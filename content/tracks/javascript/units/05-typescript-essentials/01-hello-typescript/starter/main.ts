// Your first .ts file. Same JavaScript underneath — now with annotations.

// 1. Annotate learner (: string) and streak (: number).
const learner = "Ada";
const streak = 12;

// 2. describeStreak(name, days) — typed params, return type string.
//    Returns `${name} has a ${days}-day streak`.

// 3. motto stays exactly as-is: inference already knows it's a string.
const motto = "TypeScript is JavaScript with types";

// 4. Print the streak line, then motto.
